<%= form_with(model: peer_review_question, local: true) do |form| %>
  <% if peer_review_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(peer_review_question.errors.count, "error") %> prohibited this peer_review_question from being saved:</h2>

      <ul>
      <% peer_review_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question %>
    <%= form.text_field :question, id: :peer_review_question_question %>
  </div>

  <div class="field">
    <%= form.label :exercise_type_id %>
    <%= form.text_field :exercise_type_id, id: :peer_review_question_exercise_type_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
