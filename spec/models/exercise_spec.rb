require 'rails_helper'

RSpec.describe Exercise, type: :model do
  describe "Exercise" do
    it 'recognises the exercise type' do
      pending("should test method find_type")
      fail
      #multiple tests for different types?
    end

    it 'tests whether the exercise complies in sandbox' do
      pending("should test method test_exercise_in_sandbox")
      fail
    end
  end
end
